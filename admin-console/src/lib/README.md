# Lib Layer

- `api/`: typed API client interface and concrete implementations.
- `mocks/`: local mock backend state/data for offline local testing.
- `auth/`: browser localStorage auth token/profile helpers.
- `hooks/`: reusable async loading hook with error/refresh handling.
- `format.ts`: shared formatting helpers.
