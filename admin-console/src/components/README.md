# Components

- `ui/`: design-system primitives (`Button`, `Card`, `Badge`, `Modal`, `DataTable`, form fields, loading skeleton).
- `layout/`: sidebar + top bar + app shell.
- `auth/`: auth guard component.
- `common/`: domain-focused reusable widgets (confidence badge, precision badge, sensitive action modal).
