# Map Abstraction

This package defines an SDK-agnostic map provider interface.

Use this to keep map rendering swappable between Google Maps SDK and Mapbox
without rewriting domain/ui business logic.
