# UI Package

Jetpack Compose screens and ViewModels.

Structure:
- `enrollment`: explicit consent workflow.
- `dashboard`: inventory/check-in/location summary.
- `lostmode`: visible incident mode controls.
- `audit`: immutable event timeline view.

ViewModels depend on domain use cases and expose UI state flows.
